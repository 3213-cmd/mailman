(ns mailman.front.themes)





(def light-theme
  {:palette {:mode "light"
             :primary {:main "#ff9800"
                       :light "#c15ee0"}
             ;; :background {
             ;; :default "#FF0000"}
             }})

(def dark-theme
  {:palette {:mode "dark"
             :primary {:main "#c15ee0"}
             :background {:default "#050505"}}})

